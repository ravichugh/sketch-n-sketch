/*
console.log("blah");
var outputCanvas = document.getElementsByClassName("outputCanvas")[0];
console.log(outputCanvas);
outputCanvas.addEventListener("scroll", function() {
  console.log("top: " + outputCanvas.scrollTop);
  console.log("left: " + outputCanvas.scrollLeft);
});
*/
